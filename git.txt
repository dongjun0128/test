git init

git remote add origin (repository address)

//Push 전엔 Pull이 있다!

git pull origin master //지정 해놓은 Repository에서 끌어온다. - Pull

git status //현재 Git과 local폴더의 싱크를 확인 -Push

git add . //변경된 파일들을 모두 Tracking -Push

git commit -m "message" //commit달기 -Push

git push origin +master  -Push

